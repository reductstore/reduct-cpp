name: "install-conan"
description: "Install conan package manager and create default profile"
runs:
  using: "composite"
  steps:
    - name: Install conan
      shell: bash
      run: pip3 install conan==2.11.0

    - name: Create profile
      shell: bash
      run: conan profile detect
